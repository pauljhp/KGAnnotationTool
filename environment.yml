name: annotation_env
channels:
  - defaults
dependencies:
  - python==3.10
  - pip
  - pandas
  - numpy
  - pip:
    - sentencepiece
    - streamlit
    - torch
    - transformers
prefix: /anaconda/envs/annotation_env